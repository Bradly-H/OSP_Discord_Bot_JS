const NO_ACCESS = 0;
const VIEWER = 1;
const SUGGESTER = 2;
const EDITOR = 3;
const OVERWRITE = 4;

const NUM_TO_ROLE = ['No Access', 'Reader', 'Commenter', 'Editor'];

module.exports = {
    NO_ACCESS,
    VIEWER,
    SUGGESTER,
    EDITOR,
    OVERWRITE,
    NUM_TO_ROLE,
}